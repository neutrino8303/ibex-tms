# ── Supabase (project: gnaoybndpgbajnfntqmy) ───────────────────────────────

# Runtime (Vercel + serverless): Session pooler, port 6543
# Supabase → Connect → ORMs → Prisma → Session mode
DATABASE_URL="postgresql://postgres.gnaoybndpgbajnfntqmy:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Migrations only (local CLI: pnpm db:deploy): Direct connection, port 5432
DIRECT_URL="postgresql://postgres:[PASSWORD]@db.gnaoybndpgbajnfntqmy.supabase.co:5432/postgres"

# Local dev (both can use direct if pooler is not needed):
# DATABASE_URL="postgresql://postgres:[PASSWORD]@db.gnaoybndpgbajnfntqmy.supabase.co:5432/postgres"
# DIRECT_URL="postgresql://postgres:[PASSWORD]@db.gnaoybndpgbajnfntqmy.supabase.co:5432/postgres"

NEXT_PUBLIC_SUPABASE_URL="https://gnaoybndpgbajnfntqmy.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Auth cookies (min 32 chars). Generate: openssl rand -base64 32
SESSION_SECRET="change-me-to-a-random-string-at-least-32-characters-long"
